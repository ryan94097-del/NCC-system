# NCC 案件賣場搜尋核對系統 — 開發與執行重點紀錄

本文件紀錄了「NCC 案件賣場搜尋核對系統」的開發歷程、系統架構以及執行優化重點，方便日後維護與參考。

---

## 📂 檔案與路徑資訊

*   **專案目錄位置**：`c:\Users\Ryan\Documents\Antigravity folder\NCC surveillance`
*   **系統主程式**：`app.py` ([點此開啟](file:///c:/Users/Ryan/Documents/Antigravity%20folder/NCC%20surveillance/app.py))
*   **環境套件定義檔**：`requirements.txt` ([點此開啟](file:///c:/Users/Ryan/Documents/Antigravity%20folder/NCC%20surveillance/requirements.txt))
*   **開發紀錄檔**：`NCC_搜尋系統開發紀錄.md` (本檔案)

---

## 🛠️ 開發歷程與迭代優化

### 階段一：基礎功能建立 (CSV 支援)
1.  **檔案上傳**：使用 `st.file_uploader` 支援 CSV 檔案上傳，智慧偵測並相容 `utf-8-sig`、`cp950` (Big5) 等常見的台灣編碼格式。
2.  **核心邏輯**：過濾出使用者設定的年份 (NCC ID 的第 5、6 碼為年份，例如 26 代表 2026 年)，並使用 DuckDuckGo Search 嘗試搜尋 `site:momo.com.tw OR site:tw.buy.yahoo.com`。

### 階段二：多 Sheet Excel 支援與智慧解析
1.  **結構相容**：使用者上傳真實 Excel 檔案 `RCB工服委託登錄表_TUV_2026.xlsx` 時，系統遭遇「找不到必要欄位」的錯誤。
2.  **智慧表頭偵測**：分析發現該 Excel 檔案的第一列為大標題，真正的欄位表頭在第 2 列 (Row 1)。因此，系統升級為**自動掃描前幾列，動態尋找同時包含「證書編號」和「型號」的列作為真正表頭**。
3.  **多 Sheet 自動分類**：系統自動偵測並分流處理包含 `LPD` (低功率射頻) 與 `TTE` (電信終端) 的工作表。
4.  **無效資料清洗**：過濾掉年份分隔列 (如只有一個整數 2018)、NCC 請款彙總列等無效行，只保留以 `CC` 開頭的有效 NCC 認證案件。

### 階段三：目標有效連結數 (達標即停) 
1.  **需求優化**：為免搜尋過多無效案件，在左側邊欄為 `LPD` 與 `TTE` 獨立新增「目標有效連結數」的輸入設定。
2.  **效率提升**：當某個分類（如 LPD）找到的有效賣場連結數已達設定上限時，**立即停止該分類的後續搜尋**，大幅節省時間並避免 IP 被封鎖。

### 階段四：搜尋引擎精準度優化 (突破 DuckDuckGo 限制)
1.  **問題發現**：DuckDuckGo 對台灣電商的網域 (site:) 索引能力極差，且 MOMO 商品頁採用前端 JavaScript 渲染，導致原先搜尋策略幾乎找不到任何商品（例如 `EDIMAX BT-8530` 均回傳 0 筆）。
2.  **多源漸進式搜尋策略**：
    *   **Yahoo 購物**：直接向 Yahoo 搜尋頁面發送請求，解析 HTML 程式碼並抓取正規的 `gdsale` 商品連結（實測可以精準抓到訊舟的藍牙收發器商品）。
    *   **MOMO 購物**：因 MOMO 採取嚴格的反爬蟲與 JS 渲染，系統智慧改為**自動生成搜尋網址**，以「MOMO搜尋『品牌+型號』（點擊查看）」的可點擊連結呈現在結果表格中，方便使用者一鍵比對。
    *   **DuckDuckGo 廣域搜尋**：不設網域限制地進行搜尋，再從結果中主動過濾篩選包含 momo / yahoo 關鍵字的連結。
    *   **動態搜尋日誌**：在開始搜尋時，介面會動態即時輸出 `📋 搜尋日誌`，透明呈現每筆商品在各管道的搜尋歷程。

---

## ⚙️ 系統參數與執行方式

### 1. 執行應用程式
在終端機 (Powershell) 中，於專案目錄下執行：
```powershell
streamlit run app.py
```
本機瀏覽網址：[http://localhost:8501](http://localhost:8501)

### 2. 重要控制參數 (側邊欄設定)
*   **目標年份過濾**：例如過濾 `26` 年 (2026年) 或 `25` 年 (2025年) 的認證案件。
*   **每筆案件搜尋結果上限**：單筆 NCC ID 最多顯示幾個符合的賣場連結（預設 3）。
*   **目標有效連結數**：當 `LPD` 或 `TTE` 累計找到足夠的連結後就自動停止搜尋。
*   **搜尋間隔延遲 (秒)**：每次搜尋間強制延遲（預設 2.0 秒），防止賣場或搜尋引擎封鎖您的 IP。

---

## 📝 備註與建議
*   **PChome 排除**：系統於爬取、廣域搜尋與清洗網址的過程中，皆寫有雙重保險邏輯，已徹底排除 `pchome.com.tw` 的商品結果。
*   **編碼處理**：匯出搜尋結果時使用 `utf-8-sig` 編碼，確保直接使用 Excel 開啟 CSV 檔時，繁體中文不會出現亂碼。
